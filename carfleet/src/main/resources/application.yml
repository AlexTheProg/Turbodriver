server:
  port: 8081

spring:
  application:
    name: carfleet
  datasource:
    password: alex1999
    url: jdbc:postgresql://localhost:5432/carfleet
    username: alex
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true


